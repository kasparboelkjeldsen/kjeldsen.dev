name: kjeldsen.dev
description: Infrastructure for kjeldsen.dev
runtime: dotnet
config:
  pulumi:autonaming:
    value:
      # Every resource in this program has an explicit, stable Azure name, because the stack was
      # adopted from resources that already existed. Auto-naming would append random suffixes and
      # propose replacing live resources.
      mode: disabled
